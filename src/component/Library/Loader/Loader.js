import React        from 'react';import {Transition} from 'react-spring/renderprops';import './styles.scss';export default function Loader(props) {    const bools = [1,2,3,4,5];    return(        <Transition            items={props.show}            from={{ opacity: 0 }}            enter={{ opacity: 1 }}            leave={{ opacity: 0 }}>            {show => show &&(props =>                <div style={props} className="container">                    {bools.map(n => <div key={`item-${n}`} className={`item-${n}`} />)}                </div>)            }        </Transition>    )}